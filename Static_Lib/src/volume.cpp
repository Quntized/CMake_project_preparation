#include <iostream>
#include "volume.hpp"

int calculateVolume(int width, int height, int depth){
    int result = width* height* depth;
    std::string stiff = "Volume calculateion would maybe : ";
    return result;
}
